# Release Preparation

## Fictional Release

Project:

```text
demo-tool
```

Version:

```text
v1.4.0
```

Release scope:

- add `audit --cache-dir <path>`
- keep `audit --json` stdout valid JSON
- route JSON-mode warnings to stderr
- update README examples
- add regression tests for CLI output behavior

## Maintainer Question

Is this release ready to tag, and what should be checked before publishing?
