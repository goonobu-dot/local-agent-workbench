# Pane 6 - Follow-Up Issues

## Suggested Follow-Ups

1. Add docs for cache cleanup behavior.
2. Add examples for CI usage with `--json`.
3. Consider a `--no-cache` flag if users ask for fully stateless audit runs.
4. Add a troubleshooting section for permission-denied cache paths.

## Not Required For This Release

These can be tracked after `v1.4.0` if the release gates pass.
